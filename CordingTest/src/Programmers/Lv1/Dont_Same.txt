1. 연속되는 숫자 제거한 이후의 배열 크기가 알수없는 상태, 따라서 ArrayList 형태의 객체를 생성한다.
2.  임의의 value값을 설정한다 (value는 범위조건에 들어가지 않는것으로 선언)
3.  이전값과 비교한후 같지않으면 배열에 추가, 아니면 추가하지않는 반복문을 만든다./
4. 그후 생성된 배열을 stream문을 통하여 배열로 변환한다. 